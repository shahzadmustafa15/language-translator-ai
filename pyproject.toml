[project]
name = "lingua-translation-api"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.140.0",
  "httpx==0.28.1",
  "langdetect==1.0.9",
  "uvicorn==0.35.0"
]

[tool.vercel]
